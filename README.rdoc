== Sapphire DKP

Ruby on rails evolution of Vaadin DKP

=== Ruby version
- Ruby 2.0

=== Running
First install the bundle gem

$ gem install bundle

Install dependencies
$ bundle install

Start the server
$ ./bin/rails s
